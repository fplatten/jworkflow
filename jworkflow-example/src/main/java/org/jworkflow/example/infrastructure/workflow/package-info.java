/**
 * Adapters connecting typed order commands and results to workflow events and registered listeners. The
 * dispatcher invokes application behavior; result events drive the configured workflow.
 */
package org.jworkflow.example.infrastructure.workflow;
