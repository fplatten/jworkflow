/**
 * Executable in-memory and PostgreSQL order examples. PostgreSQL configuration is read from the
 * environment; use the documented start/resume modes to demonstrate durable restart. Examples are
 * application wiring, not production payment, inventory or shipping integrations.
 */
package org.jworkflow.example;
